จากที่อาจารย์ได้ให้แบบฝึกหัด shallow equality มา ผมได้นั่งทำความเข้าใจแล้ว
สรุปการทำงานคร่าวๆคือ รับ parameter มาเป็น obj 2 ตัว
แล้วเราจะสร้างตัวแปรมารับ key ของ oืbject ทั้ง 2 อันเก็บเป็น array [ 'isbn', 'title' ]
แล้วเช็คก่อนว่า มีสมาชิก(length)ใน array เท่ากันหรือไม่่ ถ้าไม่เท่าก็ return false
ซึ่งถ้าผ่าน if เมื่อกี้มาได้หมายความว่าตอนนี้ array ทั้ง 2 อันมี length เท่ากัน
เลยต้อง loop เช็คที่ละค่าว่าเป็นค่าเดียวกันไหม โดยจะเช็คทีละ key ว่ามี value ไหนไม่เหมือนกันไหม ถ้ามี return false
ถ้าผ่านทั้งหมดนี้มาได้หมายความว่า obj 2 ตัวนี้มี value ที่เหมือนกันเป๊ะๆเลย return true
(โดยที่อาจจะเป็น obj เดียวกันที่(Reference assignment หรือที่อาจารย์อธิบายว่าเป็น roommate กัน)ก็ได้เพราะเราเช็คว่า value เหมือนกันไหม)

ต่อมาผมเลยอยากลองเขียน function นี้ให้อยู่ในรูปแบบ class method เพื่อฝึกการเขียน class
โดยเขียน function คล้ายๆเดิม แต่ด้วยความที่มันเป็น method ใน class เราเลยต้องใช้ this เพื่อชี้ไปหา object ตัวนั้นๆ
ยกตัวอย่างบรรทัด let thisKeys = Object.keys(this)
เวลาจะ output ออกมาดูเลยต้องเขียนในรูป console.log(BookA.isShallowEquality(BookB))

สำหรับโจทย์วันนี้ผมให้ chatgpt generate ออกมา 5 ข้อ

1.นับจำนวนตัวเลขซ้ำใน array
ผมใช้ reduce รวบค่าให้เป็น obj โดยให้ค่าตั้งต้นเป็น {}

(a,b) => {a[b] = (a[b] || 0) +1
    return a
}
บรรทัดนี้เป็นการ assign key ของแต่ละเลข และนับ count ทุกครั้งที่มีตัวเลขนั้น




2.กรอง array ของ object ตามเงื่อนไขหลายตัว
ผมใช้ filter เพื่อหา obj ที่มีเงื่อนไขตามโจทย์คือ price<10000 แล้วมีอยู่ใน stock

filter(a => a.price<10000 && a.stock > 0)




3.JSON.stringify() แปลง object เป็น string
ข้อนี้ไม่มีอะไรผมแค่อยากกลองใช้ stringify เพื่อแปลงเป็น obj ใน js ให้อยู่ในรูปพร้อมส่ง data เป็น JSON




4.แปลง array ของ string เป็น object แยกตัวอักษร
ข้อนี้ผมลองเขียนเองแบบ loop ธรรมดาซึ่งเป็น algorithm ที่ไม่ค่อยดีใช้หลายบรรทัดเลยให้ gpt gen เฉลยที่มีวิธีที่ดีกว่ามาด้วย

โค้ดของผม:
ดึงตัวอักษรตัวแรกของทุกตัวมาเก็บไว้ใน array ชื่อ firstLetters ซึ่งเช็คด้วยว่ามีอยู่รึยัง ถ้ายังไม่มีถึงจะ push เข้าไป
เสร็จแล้วก็ loop ให้ทุกตัวใน firstLetters เป็น {key} ที่เก็บค่าเป็น array ใน object result
แล้วก็จับคู่ว่าขึ้นต้นด้วยตัวอักษรไหน ก็ push ใส่ใน key นั้น

โค้ดของ gpt:
โค้ดนี้ใช้ reduce สร้าง object grouped โดยวนลูปผ่านคำใน array words ทีละคำ ดึงตัวอักษรตัวแรกของคำ (first = word[0]) 
มาใช้เป็น key ของ object แล้วเช็คว่า key นั้นมีอยู่ใน acc หรือยัง ถ้าไม่มีก็สร้าง array ว่างไว้ 
จากนั้น push คำปัจจุบันเข้าไปใน array ของ key นั้น 
สุดท้าย reduce คืนค่า acc เป็น object ที่เก็บคำทั้งหมดจัดกลุ่มตามตัวอักษรตัวแรกของแต่ละคำ




5.สร้างคลาส Student และคำนวณเกรด
ข้อนี้มีการคิด logic method คือหาค่าเฉลี่ยของคะแนนที่รับมากับหาเกรด
โดยหาค่าเฉลี่ยใช้ reduce จับบวกค่าใน array ทั้งหมดก่อนแล้วหารด้วย length ของ array
และใช้ ternary operator ในการแยกเกรด